# build123d-mcp 0.3.31 Retest - 2026-06-02

## Summary

`build123d-mcp` was retested after the earlier Windows execution failure report. The goal was to confirm whether simple execution works under the Codex MCP host and whether the project is usable for local CAD-agent experiments.

## Result

- Basic server startup worked.
- Simple tool calls worked.
- The previous execute-path issue was improved enough to continue experimentation.
- The tool is better positioned as a build123d execution and inspection helper than as a full industrial CAD workflow by itself.

## Notes

Useful future checks:

- Run a small multi-part assembly.
- Compare its output with the existing `cad` skill and AgentCAD workflow.
- Check whether it can expose stable structured metrics for validation gates.

